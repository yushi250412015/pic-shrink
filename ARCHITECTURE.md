# 架构与开发规范

本文档说明项目的分层架构、设计原则与开发约定，作为后续迭代的统一标准。

## 分层结构

```
src/
├── utils/        # 纯函数工具：无 DOM、无副作用、可单测
├── config.js     # 常量、默认设置、预设（单一数据源）
├── store.js      # 应用状态与订阅通知
├── pipeline.js   # Worker 池与任务调度
├── compression.js# 压缩/转换核心（Worker 内运行）
├── worker.js     # Worker 入口（薄封装）
├── ui/           # 界面模块：读取 store、渲染、绑定事件
└── main.js       # 入口：装配各模块（依赖注入）
```

依赖方向单向：`ui → store / pipeline / utils`，`pipeline → store / worker`，`utils` 不依赖任何上层。禁止反向依赖（例如 `utils` 引用 DOM 或 store）。

## 设计原则

### 1. 单一职责（SRP）

每个模块只做一件事。例如 `utils/bytes.js` 只处理字节格式化；`ui/stats-bar.js` 只负责统计栏渲染。一个函数若同时承担「计算 + 渲染 + 副作用」，就应拆分。

### 2. 封装与低耦合

- 模块通过**显式接口**（导出的函数/类方法）交互，不直接读写彼此的私有状态。
- `store.js` 只暴露 `getState / subscribe / setXxx` 等方法，外部无法直接篡改内部 `items`。
- UI 模块之间**互不引用**，通过 store 订阅与回调协作。
- `Pipeline` 与具体压缩逻辑解耦：它只调度任务，压缩细节在 `compression.js`。

### 3. 纯函数与副作用分离

- `utils/` 下全部为纯函数：相同输入 → 相同输出，不依赖 DOM / 网络 / 时间等。
- 副作用（Worker、对象 URL、DOM 事件、下载）集中在 `ui/`、`pipeline.js`、`worker.js`。
- 好处：纯函数可确定性单测，不依赖浏览器环境。

### 4. 状态管理：单向数据流

- 状态集中在 `store.js`，修改只能通过 store 方法，修改后 `emit()` 通知订阅者重新渲染。
- UI 模块**只读状态 + 派发动作**，不维护本地副本，避免状态分叉。

### 5. 并发一致性

- 图片处理在多个 Web Worker 中并行执行。
- 每个任务携带 `rev` 版本号；用户裁剪/重压会使 `rev` 自增，旧任务回传的结果因版本不符被丢弃，避免「旧结果覆盖新结果」的竞态。

## 开发约定

- 命名：文件名 kebab-case；导出函数小驼峰；纯函数命名见名知意（如 `normalizedCropToRect`）。
- 注释：只解释「为什么」，不解释「是什么」；公开 API 用 JSDoc 标注参数与返回值。
- 测试：`utils/` 的每个纯函数都应有单元测试；改动后运行 `npm test`。
- 新增设置项流程：`config.js` 加默认值 → `index.html` 加控件（`data-setting`）→ 需要映射时在 `utils/` 加纯函数 → `compression.js` 消费 → 补测试。

## 为什么这样做

这些原则共同服务于一个目标：**改动一处，不波及他处**。分层 + 纯函数让逻辑可测试、可复用；单向数据流让界面行为可预测；解耦让模块可独立替换（例如把 UI 换成框架，`core` 与 `utils` 无需改动）。
